<UserControl x:Class="SimpleTrader.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleTrader.WPF.Views"
             xmlns:vm="clr-namespace:SimpleTrader.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:LoginViewModel, IsDesignTimeCreatable=False}" FontSize="18" Foreground="{StaticResource BrushPrimary3}">
    <UserControl.Background>
        <ImageBrush ImageSource="/Resources/login-background.jpg" Stretch="UniformToFill" Opacity="0.6"/>
    </UserControl.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="70*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" BorderBrush="{StaticResource BrushPrimary2}" BorderThickness="3" Margin="20" Padding="30" HorizontalAlignment="Center" CornerRadius="5" Background="White">
            <Border.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Simple Trader" HorizontalAlignment="Center" FontSize="40" Foreground="{StaticResource BrushPrimary3}" Margin="0 10"/>
                <TextBlock Text="The world's simplest stock trading application." HorizontalAlignment="Center" FontSize="18" FontWeight="Thin" Foreground="{StaticResource BrushPrimary3}"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 20 0 10">
                        <TextBlock Text="Username:"  Grid.Column="0" Width="100" />
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Width="200"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 10">
                        <TextBlock Text="Password:"  Grid.Column="0" Width="100"  />
                        <PasswordBox x:Name="pbPassword" Grid.Column="1" Width="200"/>
                    </StackPanel>

                    <Button Content="Login" Grid.Row="2" Grid.Column="0" Click="Login_Click" Margin="15" HorizontalAlignment="Center" IsDefault="True"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
