<UserControl x:Class="SimpleTrader.WPF.Views.BuyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleTrader.WPF.Views"
             xmlns:vm="clr-namespace:SimpleTrader.WPF.ViewModels"
              xmlns:conv="clr-namespace:SimpleTrader.WPF.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:BuyViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:EmptyStringToZeroConverter x:Key="EmptyStringToZeroConverter"/>
    </UserControl.Resources>
    <Grid Margin="20" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel HorizontalAlignment="Center">
            <TextBox Text="{Binding Symbol, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
            <Button Margin="10 0 0 0" Content="Buscar" Command="{Binding SearchSymbolCommand}" Padding="8 2"/>
        </WrapPanel>
        <WrapPanel Grid.Row="1">
            <WrapPanel.Style>
                <Style TargetType="WrapPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SearchResultSymbol}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </WrapPanel.Style>
            <Border BorderBrush="Gray" BorderThickness="1" Margin="0 20 0 0" Padding="20" CornerRadius="3 0 0 3">
                <StackPanel >
                    <TextBlock Text="{Binding SearchResultSymbol}" FontSize="32"  HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding StockPrice, StringFormat=C}" FontSize="14" Margin="0 10 0 0"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="Gray" BorderThickness=" 0 1 1 1" Margin="0 20 0 0" Padding="20" CornerRadius="0 3 3 0">
                <StackPanel>
                    <WrapPanel Margin="0 10">
                        <TextBlock Text="Acciones a comprar:" Width="150" HorizontalAlignment="Left" />
                        <TextBox Text="{Binding SharesToBuy, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource EmptyStringToZeroConverter}}" Width="50"/>
                    </WrapPanel>
                    <WrapPanel Margin="0 10">
                        <TextBlock Text="Precio por acción:" Width="150" />
                        <TextBlock Text="{Binding StockPrice, StringFormat=C}" />
                    </WrapPanel>
                    <TextBlock Margin="0 10" Text="{Binding TotalPrice, StringFormat=C}" FontSize="32"  HorizontalAlignment="Center"/>
                    <Button Content="Comprar" Command="{Binding BuyStockCommand}" Padding="8 2" Margin="0 10 0 0"/>
                </StackPanel>
            </Border>
        </WrapPanel>
        
    </Grid>
</UserControl>
